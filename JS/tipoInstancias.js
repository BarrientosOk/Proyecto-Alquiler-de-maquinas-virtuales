class TipoInstancia {
  constructor(id, nombre, stock, precioAlquiler, costoPorEncendido) {
    this.id = id;
    this.nombre = nombre;
    this.stock = stock;
    this.precioAlquiler = precioAlquiler;
    this.costoPorEncendido = costoPorEncendido;
  }
}
